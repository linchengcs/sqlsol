/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.0.1/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'kotlin'

// Apply the application plugin to add support for building an application
apply plugin: 'application'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // This dependency is found on compile classpath of this component and consumers.

    compile fileTree('lib')

    compile 'com.google.guava:guava:21.0'
    // Use JUnit test framework

    compile 'org.slf4j:slf4j-api:1.7.12'
    compile group: 'ch.qos.logback', name: 'logback-classic', version: '1.2.3'
    compile files("${System.properties['java.home']}/../lib/tools.jar")
    testCompile 'junit:junit:4.12'
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
}

// Define the main class for the application
mainClassName = 'App'

task statsAgent(type: Jar, dependsOn: classes) {
    manifest {
        attributes 'Premain-Class': 'stats.Agent'
        attributes 'Agent-Class': 'stats.Agent'
    }
    destinationDir = file('./lib')
    archiveName = "stats-agent.jar"
    from (sourceSets.main.output) {
        include("/")
    }

}
buildscript {
    ext.kotlin_version = '1.3.31'
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
compileKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
compileTestKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
